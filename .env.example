DATABASE_URL=postgresql://localhost:5433/gcal_mcp
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
ENCRYPTION_KEY=  # 32-char random string for AES-256 token encryption
APP_PORT=3040
APP_URL=http://localhost:3040
ADMIN_PASSWORD=  # password required to access the web UI (add/remove accounts)
MCP_TOKEN=  # static token required on /mcp/sse?token=... — paste into Claude's MCP URL
